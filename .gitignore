.zsh_history
.gnupg/
!.gnupg/gpg-agent.conf
!.gnupg/gpg.conf
ohmyzsh/
.vscode
.swp
*.zcompdump*
*.zsh_sessions/
.env
.DS_Store
*/karabiner/automatic_backups/*
automatic_backups/
.zcompcache/

# we don't commit this since it contains our emails, user id's org id's etc.
.claude.json
claude/.claude.json

# pi - contains API keys
pi/.pi/agent/auth.json
# pi - chat history
pi/.pi/agent/sessions/
# pi - installed packages (reinstallable via pi install)
pi/.pi/agent/git/
pi/.pi/agent/npm/
